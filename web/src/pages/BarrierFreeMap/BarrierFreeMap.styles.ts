import { css } from '../../../styled-system/css';

export const listContainer = css({
  maxW: '100vw',
  p: '0 2rem',
});

export const mapContainer = css({
  width: '100%',
  height: '100vh',

  overflow: 'hidden',
});
